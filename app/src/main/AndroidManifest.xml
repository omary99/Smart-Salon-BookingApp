<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartsaloonbookingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/salon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/salon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MySmartActivity"></activity>
        <activity android:name=".NewStyleActivity" />
        <activity android:name=".MyBookActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".MassageActivity" />
        <activity android:name=".NailsActivity" />
        <activity android:name=".ScrubActivity" />
        <activity android:name=".MakeupActivity" />
        <activity android:name=".HairstyleActivity" />
        <activity android:name=".HairdressingActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".Login_Form" />
        <activity android:name=".Signup_Form" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>